# ====================================
# Docker Compose 环境变量配置
# ====================================
# 复制此文件为 .env 并填入你的配置
# 请勿将 .env 提交到版本控制

# ====================================
# 应用配置
# ====================================
NODE_ENV=production

# 前端端口（主机端口）
FRONTEND_PORT=3002

# 后端端口（主机端口）
BACKEND_PORT=3001

# ====================================
# 跨域配置 (CORS)
# ====================================
# Docker Compose 本地开发时使用
CORS_ORIGIN=http://localhost:3002

# 生产环境部署时，设置为你的实际域名：
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com

# ====================================
# 邮件服务配置（163 邮箱）
# ====================================
# SMTP 服务器地址（163 邮箱）
SMTP_HOST=smtp.163.com

# SMTP 端口（SSL 使用 465，TLS 使用 587）
SMTP_PORT=465

# 是否使用 SSL/TLS
SMTP_SECURE=true

# 你的 163 邮箱地址
SMTP_USER=your-email@163.com

# 163 邮箱授权码（不是你的登录密码）
# 获取方式：163 邮箱设置 -> POP3/SMTP/IMAP -> 授权码
SMTP_PASSWORD=your-authorization-code

# 发件人地址
MAIL_FROM=TrendMonitor <your-email@163.com>

# ====================================
# API 身份验证
# ====================================
# 密钥用于保护 API 端点
# 请生成一个强随机字符串
API_KEY=your-secret-api-key-change-this

# API 密钥请求头名称
API_KEY_HEADER=X-API-Key

# ====================================
# 外部 API 配置
# ====================================
# UApiPro API 端点
UAPI_BASE_URL=https://uapis.cn/api/v1/misc/hotboard

# API 超时时间（毫秒）
API_TIMEOUT=15000

# 缓存持续时间（毫秒）
CACHE_DURATION=60000

# ====================================
# Swagger 文档配置
# ====================================
# 是否启用 Swagger API 文档
SWAGGER_ENABLED=true

# ====================================
# GitHub Container Registry（可选）
# ====================================
# 使用预构建镜像的 Docker Compose 生产环境
# 你的 GitHub 仓库（用户名/仓库名）
GITHUB_REPOSITORY=your-username/trendmonitor

# 使用的镜像标签（latest 或特定版本如 v1.0.0）
IMAGE_TAG=latest
